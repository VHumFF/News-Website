import React from "react";
import { Card, Grid, CardMedia, CardContent, Typography } from "@mui/material";

export default function BreakingNews({
  image = "#",
  title = "Breaking News",
  description = "Text text text text...",
}) {
  return (
    <Card>
      <Grid container>
        <Grid item xs={4}>
          <CardMedia component="img" height="290px" image={image} alt="Breaking News Image" />
        </Grid>
        <Grid item xs={8}>
          <CardContent>
            <Typography variant="h5">{title}</Typography>
            <Typography variant="body2" color="text.secondary">
              {description}
            </Typography>
          </CardContent>
        </Grid>
      </Grid>
    </Card>
  );
}
